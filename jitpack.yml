# JitPack 构建配置
jdk:
  - openjdk21

# 构建前脚本
before_install:
  - echo "开始构建 CNMoney 插件..."

# 构建脚本
install:
  - mvn clean install -DskipTests=true -Dmaven.javadoc.skip=false -B -V

# 环境变量
env:
  - MAVEN_OPTS="-Xmx1024m"
